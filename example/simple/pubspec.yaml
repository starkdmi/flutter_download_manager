name: example
description: Flutter Download Manager Example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  isolated_download_manager: '>=1.0.0 <2.0.0'
  isolated_download_manager_flutter:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true